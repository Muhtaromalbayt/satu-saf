/**
 * auth.ts - legacy BetterAuth config removed.
 * Authentication is now handled by the manual session system in lib/server/session.ts.
 * Login is done via /api/auth/sheets-login (Google Sheets based, no password).
 *
 * This file is kept as a reference stub.
 */

export { };
